const fs = require('fs');

// We have effectively undone ALL our work locally. Let's redo it quickly.
console.log("I need to re-apply the changes since `git reset --hard HEAD` wiped my uncommitted work.");
